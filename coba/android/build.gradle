buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.2.0'
    }
}
buildscript {
    repositories {
        google()
        jcenter()
    }
}
   buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.2.0'
        classpath 'com.google.gms:google-services:4.3.10'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.5.21" // Ganti dengan versi Kotlin yang valid dan tersedia
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

rootProject.buildDir = '../build'

// Konfigurasi untuk membersihkan direktori build
tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
